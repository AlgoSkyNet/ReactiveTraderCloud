import { MockScheduler } from 'rt-testing'

describe('displayCurrencyChartEpic', () => {
  it('Should only listen to display chart actions', () => {})

  it('Should call platform open with ChartIQ and map to currencyChartOpened action', () => {})
})
