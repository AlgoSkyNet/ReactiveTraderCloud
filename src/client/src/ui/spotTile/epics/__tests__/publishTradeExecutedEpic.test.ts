import { MockScheduler } from 'rt-testing'

describe('Publish Trade Executed Epic', () => {
  it('Should only listen to executed trade action', () => {})

  it('Should call platform interop with correlationId and null', () => {})
})
